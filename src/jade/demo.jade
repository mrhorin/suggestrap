doctype html
html(lang="en")
  head
    meta(charset="utf-8")
    title Suggestrap Demo
    script(src="../suggestrap.main.min.js")
    style.
      h1{ font-weight: bold; }
      body{ background-color: #f2f9ff; }
      .container{
        margin-left: auto;
        margin-right: auto;
        width: 500px;
      }
    script.
      window.onload = function(){
        req = {
          target: "target",
          url: "http://example.com/suggest/json/%QUERY",
          key: "name",
        };
        suggestrap = new window.Suggestrap(req);
      }
  body
    .container
      h1 Suggestrap Demo
      div(style="position: relative;")
        input#target(type="text")
        input(type="submit" value="submit")
